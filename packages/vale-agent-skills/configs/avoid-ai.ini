# avoid-ai-writing -- `blog` context profile (the default).
# All rules at full strength. See the tolerance matrix in the skill for how the
# other profiles relax this one.
MinAlertLevel = suggestion

[*]
BasedOnStyles = AvoidAI
Vale.Repetition = YES
