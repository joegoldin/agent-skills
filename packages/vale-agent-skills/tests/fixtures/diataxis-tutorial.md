# How to configure logging

In this tutorial you will learn about the logger. By the end of this tutorial you
should be able to see output. Optionally, you may want to use JSON instead.
Alternatively, feel free to pick a different sink, depending on your setup.

The reason for this is that the writer buffers. Under the hood, it uses a ring.
Hopefully the file appears; in most cases it usually does.
